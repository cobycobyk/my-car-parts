const fetch = require('node-fetch');
const yearURL = 'https:///api.car.com';
const makeURL = 'https:///api.car.com';
const modelURL = 'https:///api.car.com';

module.exports = {
    index,
};

function index(req, res) {
    
};